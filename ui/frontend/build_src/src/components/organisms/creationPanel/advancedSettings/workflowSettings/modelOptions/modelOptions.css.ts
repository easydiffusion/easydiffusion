import { style } from '@vanilla-extract/css';

export const modelOptionsMain = style({
  display: 'flex',
});

export const modelOptionsDropdown = style({
});

export const modelOptionsSection = style({
});
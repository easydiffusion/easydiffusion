# Testing Instructions

## Running Tests

**Run the tests using the virtual environment's Python:**

```bash
# On Windows
.venv\Scripts\python.exe -m pytest tests/ -v

# On Linux/Mac
.venv/bin/python -m pytest tests/ -v
```

All tests should pass when the virtual environment is properly set up with the required dependencies from `requirements.txt`.
